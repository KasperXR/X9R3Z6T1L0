using System.Collections;
using System.Collections.Generic;
using UnityEngine;

using System.IO;
using Dummiesman;

public class OBJImporter : MonoBehaviour
{
    /*
    public GameObject objContainer; // Reference to the container object in the scene
    public GameObject button; // Reference to the button object in the scene
    void Start()
    {
        button.GetComponent<UnityEngine.UI.Button>().onClick.AddListener(OpenFileBrowser);
    }

    public void OpenFileBrowser()
    {
       // string filePath = EditorUtility.OpenFilePanel("Select OBJ File", "", "obj");

        if (!string.IsNullOrEmpty(filePath))
        {
            LoadOBJ(filePath);
        }
    }

    void LoadOBJ(string filePath)
    {
        // Load the OBJ file
        GameObject loadedObject = new OBJLoader().Load(filePath);

        if (loadedObject != null)
        {
            // Set the parent object for the loaded object
            loadedObject.transform.SetParent(objContainer.transform);
        }
    }
    */
}